# todo

1. class **RBTreeNode extends BinaryTreeNode** {...}
2. class **RBTree extends BinarySearchTree** {...}
    21. унаследовать класс с заменой узлов с BinaryTreeNode на RBTreeNode
    22. переопределить **insertValue()**
    23. унаследовать **getTreeDepth()**
        221. унаследованный метод должен работать с узлами не ~~BinaryTreeNode~~, а с **RBTreeNode**